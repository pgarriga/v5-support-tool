Review the current state of the project and update both CLAUDE.md and README.md to reflect any changes that have been made since they were last written.

Steps:
1. Read the current CLAUDE.md and README.md
2. Read the current state of all relevant source files: src/App.vue, src/router.ts, src/types.ts, src/main.ts, src/views/*.vue, src/composables/*.ts, src/css/main.css, package.json
3. Identify what has changed or is outdated in each doc (new views, new composables, changed routes, new features, renamed files, stack changes, etc.)
4. Update CLAUDE.md to accurately reflect the current architecture, file structure, routes, components, composables, and CSS classes. This file is for Claude — focus on technical detail useful for development.
5. Update README.md to accurately reflect the current features, stack, file structure, and routes. This file is for humans — focus on what the tool does and how to use it.
6. Only change sections that are actually outdated. Do not rewrite content that is still accurate.
